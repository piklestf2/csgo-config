//Binds
bind o "exec autoexec"
bind b "buymenu"
bind c "radio3"
bind g "drop"
bind m "teammenu"
bind r "+reload"
bind u "messagemode2"
bind v "+voicerecord"
bind y "messagemode"
bind z "radio1"
bind ` "toggleconsole"
bind ESCAPE "cancelselect"
bind left spec_prev
bind right spec_next

bind p movementtoggle

bind tab +scoreboard

bind space "+crouchjump"
bind ctrl "+duck"
bind shift "+slow_walk"
bind f "+radar"
bind h "+ez_skins"
bind 1 "equip_primary"
bind 2 "equip_secondary"
bind 3 "equip_tertiary"
bind 4 "equip_grenade"
bind 5 "equip_bomb"
bind 6 "equip_fire"
bind q "equip_smoke"
bind e "equip_flash"

equip_primary
regbuys

//Offline
bind n bot_place
bind i trajectorytoggle
